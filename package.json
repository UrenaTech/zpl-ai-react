{
  "name": "zpl-api",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "check": "npm run check --workspace @urenatech/zpl-core && npm run check --workspace @urenatech/zpl-react && npm run check --workspace @urenatech/zpl-angular && npm run check --workspace @urenatech/zpl-vue"
  }
}
